# Changelog

本插件的所有重要变更都记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-08-02

首个公开发布版本。为 InkCanvasForClass（社区版，.NET 10 构建）提供随机抽选 / 快抽结果的自动语音播报与音效增强。

### 新增

- **自动播报**：随机抽选 / 快抽完成后自动朗读被抽中的姓名或学号；连续触发自动截断当前播报并立即播报最新内容（打断与抢占机制）。
- **多 TTS 引擎**：
  - Windows SAPI（本地，默认）
  - Windows Media（本地，`Windows.Media.SpeechSynthesis`；未打包宿主可能因缺少应用包身份不可用）
  - Edge 在线语音（非官方接口，支持超时设置与失败自动降级到本地引擎）
- **播报增强**：`{name}` 占位符模板、播报延迟、学号逐位朗读（`2024` → `2 0 2 4`）。
- **自定义读音**：多音字 / 生僻字字面量替换规则，支持拖拽排序，最多 100 条。
- **提示音**：朗读前的前奏音与朗读后的后置音，支持 WAV / MP3 / FLAC，受全局打断机制控制。
- **多入口截断**：白板工具栏与浮动工具栏「播报截断」按钮、「更多 / 工具」菜单项、全局热键（在宿主「快捷键设置」页内配置组合键）。
- **系统托盘**：「自动播报」开关菜单项。
- **课堂自动化集成**：4 个行动（切换引擎 / 音色、调整语速、调整播报模板、开启 / 关闭自动播报）与 2 条规则（TTS 播报中、当前 TTS 引擎）。
- **播报音频缓存**：按引擎、发音人、语速、音量、文本缓存已合成音频；支持按容量 LRU / 按保留天数 / 不限制三种策略。
- **预缓存**：启动时或手动按名单范围预合成播报音频；切换名单后自动清空旧缓存（名单指纹检测）。
- **随机发音人**：每次播报从中文发音人中随机挑选。
- **无缝设置**：设置面板嵌入宿主「插件设置」，修改实时自动保存并热生效；内置「播放播报示例」试听。

### 修复

- 自动化「TTS 播报中」规则即时刷新失效：宿主 `RulesetService.NotifyStatusChanged()` 为实例方法，原按静态方法调用导致每次播报触发两条错误日志；已改为经 `AutomationBootstrap.Service.RulesetService` 解析实例后调用，规则即时生效。
- 「播报音频缓存」在启动时关闭的情况下，会话内于设置页开启缓存完全无效（缓存与预缓存对象未创建）；现在始终创建实例、由开关门控，且开启时补做名单变更检测。
- 语音合成失败（非取消）遗留的临时文件堆积：现失败路径立即删除，不再等待下次启动清理。
- 插件关闭时未取消正在播放的示例试听，导致关闭后残留播放与误报错误日志。
- 缓存策略变更触发的文件淘汰可能阻塞 UI 线程（设置热生效路径）：淘汰已移至后台执行。
- SAPI 引擎可用性恒报可用：现如实反映初始化结果（ProgID 缺失 / COM 激活失败时设置页明确提示「系统 SAPI 组件不可用」）。
- 托盘菜单注入无自愈：宿主托盘图标资源晚于插件初始化时永久缺失；现注入失败每秒重试，成功或开关关闭后停止。
- 抽选历史监控在历史文件被整体重写（恢复备份 / 损坏重建）时可能误播最多 10 条旧名字：现静默重新对齐基线。
- 配置迁移保存任务未观察异常（fire-and-forget 的任务故障可能触发未处理异常事件）。

### 其他

- 移除无调用方的死代码（`TtsProviderManager` 的 `ITtsEngine` 实现与写后无人读的配置状态、`VoiceQueue.PendingCount`）。
- 新增 GitHub Actions 构建工作流与 `v*` 标签发布工作流；宿主 SDK / 控件按 `net10.0-windows10.0.19041.0` 覆盖构建（上游社区仓库仍为 net6.0 目标）。
- README 按 ICC-CE 插件规范重写；采用 GPL-3.0 许可（与宿主一致）。
